# =============================================================================
# Development Environment Variables
# =============================================================================
# Copy this file to .env in the project root and modify as needed.
# =============================================================================

# Server Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Service Mode
# Set to 'true' to use real services, 'false' for in-memory stubs
USE_REAL_SERVICES=false

# PostgreSQL Configuration
DB_USER=dam
DB_PASS=dam
DB_NAME=dam
DATABASE_URL=postgresql://dam:dam@localhost:5432/dam

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# MinIO/S3 Configuration
S3_ENDPOINT=http://localhost:9000
S3_BUCKET=dam-assets
S3_PUBLIC_ENDPOINT=http://localhost:9000
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio123

# AWS Credentials (for S3-compatible services)
AWS_ACCESS_KEY_ID=minio
AWS_SECRET_ACCESS_KEY=minio123
AWS_REGION=us-east-1

# Storage Base URL
STORAGE_BASE_URL=s3://bucket

# Frontend Configuration
VITE_API_URL=http://localhost:3000
VITE_S3_ENDPOINT=http://localhost:9000
